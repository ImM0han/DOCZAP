import React from "react";
import { View, Text } from "react-native";

export default function MergeScreen() {
  return (
    <View className="flex-1 px-5 pt-4">
      <Text className="text-[20px] font-extrabold text-text">Merge 🔗</Text>
      <Text className="text-muted mt-2 font-semibold">Drag reorder + merge logic in Phase 4.</Text>
    </View>
  );
}